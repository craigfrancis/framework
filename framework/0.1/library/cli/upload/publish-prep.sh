#!/bin/bash

#--------------------------------------------------
# Config
#--------------------------------------------------

	DST_PATH="$1";

	if [[ -z "${DST_PATH}" ]]; then
		echo "Missing parameters";
		echo;
		exit;
	fi

#--------------------------------------------------
# Ensure all files exist
#--------------------------------------------------

	pwd;
	echo "${DST_PATH}";
