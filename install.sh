
#-------------------------------------------------
# Server
#-------------------------------------------------

	if [ -z $1 ]; then
		SERVER="stage";
	else
		SERVER="$1";
	fi

#-------------------------------------------------
# Server specific
#-------------------------------------------------

	#if [ $SERVER = "stage" ]; then
	#fi

	#if [ $SERVER = "live" -o $SERVER = "demo" ]; then
	#fi

#-------------------------------------------------
# Permissions
#-------------------------------------------------

	find ./files/ -type d -exec chmod 777 {} \;
	find ./files/ -type f -exec chmod 666 {} \;
